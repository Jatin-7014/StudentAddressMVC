@model  CRUD_Student_Address.Models.Address

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>GetStudentAddressById</title>

</head>
<body>
    <div>
        <h4>Address</h4>
        <hr />
        @if (Model.State == null)
        {
            <h3> Address doesnt exist for this student</h3>
        }
        else
        {
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.City)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.City)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.State)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.State)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Country)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Country)
                </dd>

            </dl>
        }
    </div>
    <p>
        @Html.ActionLink("Student List", "Index") |
        @Html.ActionLink("Edit Address", "EditAddress", new { id = Model.Id })
        @Html.ActionLink("Delete Address", "DeleteAddress", new { id = Model.Id })

    </p>

</body>
</html>